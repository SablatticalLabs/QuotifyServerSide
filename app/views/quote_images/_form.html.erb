<%= form_for(@quote_image, :url=> quote_quote_images_path(@quote)) do |f| %>
  <% if @quote_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quote_image.errors.count, "error") %> prohibited this quote_image from being saved:</h2>

      <ul>
      <% @quote_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
  	<div class="field">
      <%= f.label :name %><%= f.text_field :name %><br />
      <%= f.label :file %><%= f.file_field :image_data %>
      <%= f.hidden_field :quote_id, :value => @quote.id %>
    </div>
      <%= f.submit %>
  </div>
<% end %>
